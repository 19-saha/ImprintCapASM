## Resubmission

* Added `run_pipeline()` — the main convenience wrapper function that was
  missing from the initial submission. This is the primary user-facing
  function described in the README.
* Fixed non-ASCII characters in R/run_pipeline.R
* Fixed incorrect argument names in ASM() call within run_pipeline()

## R CMD check results

0 errors | 0 warnings | 3 notes

* NOTE: New submission
* NOTE: Unable to verify current time — network issue on local machine,
  not a package problem
* NOTE: HTML Tidy version outdated on local machine — CRAN servers will
  validate correctly
